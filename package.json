{
  "name": "personal-dev-site",
  "private": true,
  "scripts": {
    "deploy": "npx wrangler pages deploy public --project-name=personal-dev-site"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
